#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int a = 16, b = 4, c = 4, d = 2, e = 4, f = 6;
    cout << (b * c) * (f + d) / a / 2 * d - c + pow(e, b - 2 * d);
    return 0;
}
